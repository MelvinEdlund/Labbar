<Window x:Class="Labb3_Quiz.Views.Dialogs.PackOptionsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Pack Options" Height="260" Width="260"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Label Grid.Row="0" Content="Name:" FontWeight="Bold" Margin="0,0,0,5"/>
        <TextBox Grid.Row="1" Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,15"/>

        <Label Grid.Row="2" Content="Difficulty:" FontWeight="Bold" Margin="0,0,0,5"/>
        <ComboBox Grid.Row="3" SelectedItem="{Binding Difficulty}" ItemsSource="{Binding Difficulties}" Margin="0,0,0,15"/>

        <Label Grid.Row="4" Content="Time per question (seconds):" FontWeight="Bold" Margin="0,0,0,5"/>
        <Grid Grid.Row="5" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Slider Grid.Column="0" 
                    Minimum="1" 
                    Maximum="30" 
                    Value="{Binding TimePerQuestionSeconds, UpdateSourceTrigger=PropertyChanged}"
                    VerticalAlignment="Center"
                    Margin="0,0,10,0"
                    TickFrequency="1"
                    IsSnapToTickEnabled="True"
                    Height="25"/>
            <TextBlock Grid.Column="1" 
                       Text="{Binding TimePerQuestionSeconds, StringFormat='{}{0} s'}" 
                       VerticalAlignment="Center"
                       MinWidth="40"
                       TextAlignment="Right"/>
        </Grid>
    </Grid>
</Window>

