<UserControl x:Class="Labb3_Quiz.Views.ConfigurationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:fa="http://schemas.awesome.incremented/wpf/xaml/fontawesome.sharp"
             xmlns:local="clr-namespace:Labb3_Quiz.Views"
             xmlns:components="clr-namespace:Labb3_Quiz.Views.Components" 
             xmlns:viewmodel="clr-namespace:Labb3_Quiz.ViewModels" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Background="White"
             d:DataContext="{d:DesignInstance Type=viewmodel:ConfigurationViewModel}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Row="0" Grid.Column="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="auto"/>
            </Grid.ColumnDefinitions>
            <Label 
                Content="{Binding ActivePack.Name}" 
                FontWeight="Bold"
                FontSize="14"
                Grid.Column="0"
                VerticalContentAlignment="Center"
                Margin="5"/>
            <StackPanel 
                Orientation="Horizontal" 
                Grid.Column="1" 
                HorizontalAlignment="Right"
                Margin="5">
                <Button 
                    Margin="5" 
                    Width="30"
                    Height="30"
                    Command="{Binding OpenPackOptionsCommand}"
                    ToolTip="Pack Options">
                    <fa:IconBlock Icon="Sliders"/>
                </Button>
                <Button 
                    Margin="5" 
                    Width="30"
                    Height="30"
                    Command="{Binding AddQuestionCommand}"
                    ToolTip="Add Question">
                    <fa:IconBlock Icon="Plus"/>
                </Button>
                <Button 
                    Margin="5" 
                    Width="30"
                    Height="30"
                    Command="{Binding RemoveQuestionCommand}"
                    ToolTip="Remove Question">
                    <fa:IconBlock Icon="Minus"/>
                </Button>
            </StackPanel>
        </Grid>

        <components:DisplayQuestionPackComponent 
            Grid.Row="1" 
            Grid.Column="0"
            Margin="5"/>

        <components:AddQuestionComponent 
            Grid.Row="0" 
            Grid.Column="1"
            Grid.RowSpan="2"
            Margin="5"/>
    </Grid>
</UserControl>
